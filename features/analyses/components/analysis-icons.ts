import { Braces, CheckCircle2, FileInput, FileText, Quote, Scale, ScanSearch, ShieldCheck, TriangleAlert } from "lucide-react"
import type { LucideIcon } from "lucide-react"

export const ANALYSIS_STEP_ICONS: Record<string, LucideIcon> = {
  FileInput,
  FileText,
  ScanSearch,
  Braces,
  Quote,
  ShieldCheck,
  TriangleAlert,
  Scale,
  CheckCircle2,
}
