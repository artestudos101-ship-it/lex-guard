"use client"

import Link from "next/link"
import { useEffect, useMemo, useState } from "react"
import { ArrowRight, Check, ChevronRight, CircleAlert, FileCheck2, FileText, Menu, Play, Search, ShieldCheck, Sparkles, Users, X } from "lucide-react"
import { Button } from "@/components/ui/button"
import { Badge } from "@/components/ui/badge"
import { Progress } from "@/components/ui/progress"
import { ThemeToggle } from "@/components/shell/account-menu"

const documents = [
  { id: "alpha", name: "Edital Alpha", agency: "Secretaria de Administração", value: "R$ 1,8M", pages: 42, risk: "Baixo risco", score: 28, tone: "success" },
  { id: "beta", name: "Edital Beta", agency: "Prefeitura de Belo Horizonte", value: "R$ 3,2M", pages: 68, risk: "Risco moderado", score: 54, tone: "warning" },
  { id: "gamma", name: "Edital Gamma", agency: "Tribunal Regional", value: "R$ 7,4M", pages: 91, risk: "Alta atenção", score: 81, tone: "critical" },
] as const

const steps = ["Documento recebido", "Texto extraído", "Seções relevantes identificadas", "Variáveis críticas encontradas", "Evidências verificadas", "Política aplicada", "Decisão consolidada"]

function ProductPreview() {
  return <div className="relative overflow-hidden rounded-2xl border border-border/80 bg-card shadow-2xl shadow-primary/10">
    <div className="flex items-center justify-between border-b border-border px-4 py-3"><div className="flex items-center gap-2"><span className="size-2 rounded-full bg-success" /><span className="font-mono text-[10px] uppercase tracking-[0.16em] text-muted-foreground">Decision console</span></div><span className="text-xs text-muted-foreground">3 oportunidades</span></div>
    <div className="flex flex-col gap-2 p-3 sm:p-5">{documents.map((doc) => <div key={doc.id} className="flex items-center gap-3 rounded-xl border border-border/70 bg-background/70 p-3"><div className="flex size-9 shrink-0 items-center justify-center rounded-lg bg-primary/10 text-primary"><FileText className="size-4" /></div><div className="min-w-0 flex-1"><p className="truncate text-sm font-semibold">{doc.name}</p><p className="truncate text-xs text-muted-foreground">{doc.agency}</p></div><Badge variant="secondary" className={doc.tone === "success" ? "bg-success-soft text-success-foreground" : doc.tone === "warning" ? "bg-warning-soft text-warning-foreground" : "bg-critical-soft text-critical-foreground"}>{doc.tone === "success" ? "Avançar" : doc.tone === "warning" ? "Revisar" : "Não priorizar"}</Badge><span className="w-12 text-right font-mono text-sm font-semibold">{doc.score}/100</span></div>)}</div>
    <div className="flex items-center gap-2 border-t border-border bg-muted/30 px-4 py-3 text-xs text-muted-foreground"><Sparkles className="size-3.5 text-primary" /> Evidências vinculadas a cada decisão</div>
  </div>
}

function DemoExperience() {
  const [selected, setSelected] = useState<(typeof documents)[number]>(documents[1])
  const [running, setRunning] = useState(false)
  const [progress, setProgress] = useState(0)
  const [step, setStep] = useState(0)
  useEffect(() => { if (!running) return; const timer = window.setInterval(() => { setProgress((current) => Math.min(current + 14, 100)); setStep((current) => Math.min(current + 1, steps.length - 1)) }, 420); return () => window.clearInterval(timer) }, [running])
  useEffect(() => { if (progress >= 100) setRunning(false) }, [progress])
  const result = useMemo(() => progress >= 100, [progress])
  return <section id="demonstracao" className="scroll-mt-24 border-y border-border bg-muted/20 py-20 sm:py-28"><div className="mx-auto flex w-full max-w-7xl flex-col gap-10 px-4 sm:px-6 lg:px-8"><div className="max-w-2xl"><p className="font-mono text-[10px] uppercase tracking-[0.18em] text-primary">Demonstração interativa</p><h2 className="mt-3 text-balance text-3xl font-semibold tracking-[-0.04em] sm:text-5xl">Experimente o LexGuard.</h2><p className="mt-4 text-pretty leading-7 text-muted-foreground">Escolha um edital de demonstração e veja como documento, política e decisão se conectam.</p></div><div className="grid gap-5 lg:grid-cols-[0.8fr_1.2fr]"><div className="flex flex-col gap-3"><p className="text-sm font-medium">Escolha uma oportunidade</p>{documents.map((doc) => <button type="button" key={doc.id} onClick={() => { setSelected(doc); setProgress(0); setStep(0) }} className={`flex min-h-24 flex-col gap-2 rounded-xl border p-4 text-left transition-all hover:border-primary/60 ${selected.id === doc.id ? "border-primary bg-card shadow-sm" : "border-border bg-background/50"}`}><div className="flex items-center justify-between gap-3"><span className="font-semibold">{doc.name}</span><ChevronRight className="size-4 text-muted-foreground" /></div><span className="text-xs text-muted-foreground">{doc.agency}</span><span className="font-mono text-[10px] text-muted-foreground">{doc.value} · {doc.pages} páginas</span></button>)}</div><div className="overflow-hidden rounded-2xl border border-border bg-card"><div className="flex items-center justify-between border-b border-border px-4 py-3"><span className="text-sm font-semibold">Analisando {selected.name}</span><Badge variant="outline">{result ? "Concluída" : running ? "Em andamento" : "Pronta"}</Badge></div><div className="grid min-h-[25rem] gap-0 md:grid-cols-[0.9fr_1.1fr]"><div className="flex flex-col gap-4 border-b border-border p-4 md:border-b-0 md:border-r"><div className="rounded-xl border border-border bg-muted/30 p-4"><div className="flex items-center justify-between text-xs text-muted-foreground"><span>Página {result ? 37 : running ? 12 : 1}</span><span>Prévia do documento</span></div><div className="mt-4 flex min-h-48 flex-col gap-3 rounded-lg bg-background p-4 text-xs leading-5 text-muted-foreground"><div className="h-2 w-2/3 rounded bg-muted" /><div className="h-2 w-full rounded bg-muted" /><div className="h-2 w-5/6 rounded bg-muted" /><div className={`rounded border p-2 transition-colors ${result ? "border-critical/50 bg-critical-soft text-critical-foreground" : "border-border"}`}>{result ? "... garantia correspondente a 5% do valor total ..." : "... a contratada deverá apresentar ..."}</div><div className="h-2 w-3/4 rounded bg-muted" /></div></div><div className="flex flex-col gap-2"><div className="flex items-center justify-between text-xs"><span>Progresso</span><span className="font-mono">{progress}%</span></div><Progress value={progress} /></div><Button className="w-full" disabled={running} onClick={() => { setProgress(0); setStep(0); setRunning(true) }}>{running ? "Processando..." : result ? "Executar novamente" : "Analisar edital"}<Play data-icon="inline-end" /></Button></div><div className="flex flex-col gap-5 p-4"><div aria-live="polite" className="flex flex-col gap-2">{steps.map((label, index) => <div key={label} className="flex items-center gap-3 text-sm"><span className={`flex size-6 shrink-0 items-center justify-center rounded-full ${index < step || result ? "bg-success-soft text-success" : index === step && running ? "bg-info-soft text-info" : "bg-muted text-muted-foreground"}`}>{index < step || result ? <Check className="size-3.5" /> : <span className="font-mono text-[10px]">{index + 1}</span>}</span><span className={index <= step || result ? "text-foreground" : "text-muted-foreground"}>{label}</span></div>)}</div>{result ? <div className="mt-auto rounded-xl border border-warning/50 bg-warning-soft p-4"><div className="flex items-start justify-between gap-3"><div><p className="font-mono text-[10px] uppercase tracking-[0.16em] text-warning-foreground">Decisão preliminar</p><p className="mt-2 text-2xl font-semibold text-warning-foreground">REVISAR</p></div><span className="font-mono text-2xl font-semibold text-warning-foreground">54/100</span></div><p className="mt-3 text-sm leading-6 text-warning-foreground">Garantia de 5% acima da política de 3%. Evidência confirmada na página 37.</p><Button variant="outline" className="mt-4 w-full" render={<Link href="/register" />}>Ver análise completa <ArrowRight data-icon="inline-end" /></Button></div> : <div className="mt-auto rounded-xl border border-primary/20 bg-primary/5 p-4 text-sm leading-6 text-muted-foreground"><CircleAlert className="mb-2 size-4 text-primary" />O fluxo mostra apenas etapas e resultados verificáveis. Nenhum raciocínio privado é exposto.</div>}</div></div></div></div></div></section>
}

function AppSections() { return <><section id="produto" className="mx-auto grid w-full max-w-7xl gap-10 px-4 py-20 sm:px-6 sm:py-28 lg:grid-cols-[0.85fr_1.15fr] lg:px-8"><div><p className="font-mono text-[10px] uppercase tracking-[0.18em] text-primary">O problema</p><h2 className="mt-3 text-balance text-3xl font-semibold tracking-[-0.04em] sm:text-5xl">O desafio não é encontrar o edital. É saber se vale o esforço.</h2></div><div className="grid gap-4 sm:grid-cols-3 lg:pt-12">{[["Muitas páginas", "Editais extensos exigem horas de leitura antes de qualquer decisão."],["Muitas variáveis", "Prazos, garantias e exigências ficam espalhados pelo documento."],["Decisão fragmentada", "Comercial, jurídico e gestão precisam trabalhar sobre a mesma evidência."]].map(([title, body]) => <div key={title} className="border-t border-border pt-4"><h3 className="font-semibold">{title}</h3><p className="mt-2 text-sm leading-6 text-muted-foreground">{body}</p></div>)}</div></section><section id="como-funciona" className="bg-primary py-20 text-primary-foreground sm:py-28"><div className="mx-auto flex max-w-7xl flex-col gap-12 px-4 sm:px-6 lg:px-8"><div className="max-w-2xl"><p className="font-mono text-[10px] uppercase tracking-[0.18em] text-primary-foreground/70">Como funciona</p><h2 className="mt-3 text-balance text-3xl font-semibold tracking-[-0.04em] sm:text-5xl">Do documento à decisão.</h2></div><div className="grid gap-px overflow-hidden rounded-2xl border border-primary-foreground/20 bg-primary-foreground/20 sm:grid-cols-3 lg:grid-cols-6">{["Edital", "Extração", "Evidência", "Política", "Risco", "Decisão"].map((item, index) => <div key={item} className="flex min-h-32 flex-col justify-between bg-primary p-4"><span className="font-mono text-xs text-primary-foreground/60">0{index + 1}</span><span className="text-lg font-medium">{item}</span><ArrowRight className="size-4 text-primary-foreground/60" /></div>)}</div></div></section><section id="evidencias" className="mx-auto grid w-full max-w-7xl gap-10 px-4 py-20 sm:px-6 sm:py-28 lg:grid-cols-[1.05fr_0.95fr] lg:px-8"><div className="rounded-2xl border border-border bg-card p-5 shadow-sm sm:p-8"><div className="flex items-center gap-3 border-b border-border pb-5"><ShieldCheck className="size-5 text-success" /><span className="font-semibold">Toda recomendação tem uma origem.</span></div><div className="flex flex-col gap-0 pt-5">{[["RISCO", "Garantia acima da política"],["POLÍTICA", "Garantia máxima: 3%"],["EVIDÊNCIA", "Página 37 · Cláusula 14.2"],["TRECHO", "Garantia correspondente a 5% do valor total"]].map(([label, value], index) => <div key={label} className="flex gap-4 border-b border-border py-4 last:border-0"><span className="w-20 shrink-0 font-mono text-[10px] text-muted-foreground">{label}</span><span className={index === 0 ? "font-semibold text-critical" : "text-sm"}>{value}</span></div>)}</div></div><div className="flex flex-col justify-center"><h2 className="text-balance text-3xl font-semibold tracking-[-0.04em] sm:text-5xl">IA com rastreabilidade, não uma caixa-preta.</h2><p className="mt-5 max-w-lg leading-7 text-muted-foreground">Cada finding aponta para a regra aplicada, a página e o trecho original. Quando a evidência é insuficiente, o LexGuard sinaliza a revisão.</p><div className="mt-8 flex flex-wrap gap-3"><Badge variant="outline"><FileCheck2 className="mr-1.5 size-3.5" /> E1 confirmada</Badge><Badge variant="outline">E2 parcial</Badge><Badge variant="outline">E3 requer validação</Badge></div></div></section><section id="colaboracao" className="border-y border-border bg-muted/20 py-20 sm:py-28"><div className="mx-auto flex max-w-7xl flex-col gap-10 px-4 sm:px-6 lg:px-8"><div className="max-w-2xl"><h2 className="text-balance text-3xl font-semibold tracking-[-0.04em] sm:text-5xl">Uma decisão. Uma equipe.</h2><p className="mt-4 leading-7 text-muted-foreground">Comercial, analista, jurídico e gestão trabalham sobre o mesmo contexto verificável.</p></div><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{[[Users,"Comercial","Priorize oportunidades"],[Search,"Analistas","Valide evidências"],[ShieldCheck,"Jurídico","Revise conflitos"],[FileCheck2,"Gestão","Aprove decisões"]].map(([Icon, title, body]) => <div key={title as string} className="rounded-xl border border-border bg-card p-5"><Icon className="size-5 text-primary" /><h3 className="mt-8 font-semibold">{title as string}</h3><p className="mt-2 text-sm text-muted-foreground">{body as string}</p></div>)}</div></div></section></> }

export function LandingPage() { const [menuOpen, setMenuOpen] = useState(false); return <main className="min-h-dvh bg-background text-foreground"><header className="sticky top-0 z-20 border-b border-border/70 bg-background/90 backdrop-blur"><div className="mx-auto flex h-16 max-w-7xl items-center justify-between px-4 sm:px-6 lg:px-8"><Link href="/" className="flex items-center gap-2 font-semibold tracking-tight"><span className="flex size-8 items-center justify-center rounded-lg bg-primary text-primary-foreground"><ShieldCheck className="size-4" /></span>LexGuard</Link><nav className={`${menuOpen ? "absolute inset-x-0 top-16 flex border-b border-border bg-background p-4" : "hidden"} flex-col gap-4 text-sm text-muted-foreground md:static md:flex md:flex-row md:items-center md:border-0 md:bg-transparent md:p-0`}><a href="#produto" onClick={() => setMenuOpen(false)}>Produto</a><a href="#como-funciona" onClick={() => setMenuOpen(false)}>Como funciona</a><a href="#evidencias" onClick={() => setMenuOpen(false)}>Evidências</a><a href="#colaboracao" onClick={() => setMenuOpen(false)}>Colaboração</a></nav><div className="hidden items-center gap-1 sm:flex"><ThemeToggle /><Button variant="ghost" size="sm" render={<Link href="/login" />}>Entrar</Button></div><div className="flex items-center gap-1 sm:hidden"><ThemeToggle /><button type="button" aria-label={menuOpen ? "Fechar menu" : "Abrir menu"} aria-expanded={menuOpen} className="flex size-10 items-center justify-center rounded-lg border border-border md:hidden" onClick={() => setMenuOpen((open) => !open)}>{menuOpen ? <X className="size-4" /> : <Menu className="size-4" />}</button></div></div></header><section className="mx-auto grid min-h-[calc(100dvh-4rem)] w-full max-w-7xl items-center gap-12 px-4 py-16 sm:px-6 md:py-20 lg:grid-cols-[0.9fr_1.1fr] lg:px-8"><div className="max-w-xl"><Badge variant="outline" className="gap-2"><span className="size-1.5 rounded-full bg-success" />Copiloto de risco para licitações</Badge><h1 className="mt-6 text-balance text-4xl font-semibold tracking-[-0.06em] sm:text-6xl">Transforme editais complexos em decisões claras.</h1><p className="mt-6 max-w-lg text-pretty text-lg leading-8 text-muted-foreground">Encontre riscos, evidências e conflitos com a política da sua empresa antes de investir horas da sua equipe.</p><div className="mt-8 flex flex-col gap-3 sm:flex-row"><Button size="lg" render={<a href="#demonstracao" />}>Testar demonstração <ArrowRight data-icon="inline-end" /></Button><Button size="lg" variant="outline" render={<Link href="/register" />}>Criar conta grátis</Button></div><p className="mt-5 text-xs text-muted-foreground">Experimente sem cadastro. Veja o produto antes de decidir.</p></div><ProductPreview /></section><DemoExperience /><AppSections /><section id="seguranca" className="bg-primary py-20 text-primary-foreground sm:py-28"><div className="mx-auto flex max-w-7xl flex-col items-start gap-8 px-4 sm:px-6 lg:flex-row lg:items-center lg:justify-between lg:px-8"><div className="max-w-2xl"><h2 className="text-balance text-3xl font-semibold tracking-[-0.04em] sm:text-5xl">Pare de analisar oportunidades às cegas.</h2><p className="mt-4 leading-7 text-primary-foreground/75">Experimente o LexGuard com uma análise demonstrativa ou crie sua própria conta.</p></div><div className="flex w-full flex-col gap-3 sm:w-auto sm:flex-row"><Button variant="secondary" size="lg" render={<a href="#demonstracao" />}>Testar demonstração</Button><Button size="lg" className="bg-background text-foreground hover:bg-background/90" render={<Link href="/register" />}>Criar minha conta</Button></div></div></section><footer className="border-t border-border"><div className="mx-auto flex max-w-7xl flex-col gap-4 px-4 py-8 text-sm text-muted-foreground sm:flex-row sm:items-center sm:justify-between sm:px-6 lg:px-8"><span className="font-semibold text-foreground">LexGuard</span><span>Decisões baseadas em evidências.</span><div className="flex gap-4"><Link href="/login" className="hover:text-foreground">Entrar</Link><Link href="/register" className="hover:text-foreground">Criar conta</Link></div></div></footer></main> }

export default LandingPage

// Dial read: DESIGN_VARIANCE 6, MOTION_INTENSITY 4, VISUAL_DENSITY 4. The asymmetry gives the product preview priority while the demo motion communicates processing state.
