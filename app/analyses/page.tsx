"use client"

import Link from "next/link"
import { useMemo, useState } from "react"
import { ArrowUpRight, CheckCircle2, Clock3, FileText, Filter, MessageSquare, MoreHorizontal, Paperclip, Plus, Search, Send, ShieldAlert, Sparkles } from "lucide-react"
import { AppShell } from "@/components/shell/app-shell"
import { Badge } from "@/components/ui/badge"
import { Button } from "@/components/ui/button"
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"
import { Input } from "@/components/ui/input"
import { ScrollArea } from "@/components/ui/scroll-area"
import { Separator } from "@/components/ui/separator"
import { MOCK_ANALYSES } from "@/mock/analyses"

const chats = MOCK_ANALYSES.map((analysis, index) => ({ ...analysis, status: index === 0 ? "Em revisão" : index === 1 ? "Concluída" : "Aguardando evidências", risk: [68, 24, 51][index], unread: index === 0 ? 3 : 0, lastMessage: index === 0 ? "Encontrei 2 pontos que merecem sua validação." : index === 1 ? "O pacote de decisão está pronto para exportação." : "Aguardando documentos complementares." }))

export default function AnalysesPage() {
  const [selectedId, setSelectedId] = useState(chats[0]?.id)
  const [query, setQuery] = useState("")
  const selected = chats.find((chat) => chat.id === selectedId) ?? chats[0]
  const filtered = useMemo(() => chats.filter((chat) => `${chat.title} ${chat.orgao}`.toLowerCase().includes(query.toLowerCase())), [query])
  return <AppShell title="Minhas análises" description="Cada análise é um espaço de conversa, evidência e decisão" action={<Button size="sm" render={<Link href="/analyses/new" />}><Plus data-icon="inline-start" /> Nova análise</Button>}>
    <div className="mx-auto flex max-w-[1500px] flex-col gap-5">
      <div className="flex flex-col gap-2"><p className="font-mono text-[10px] uppercase tracking-[0.18em] text-primary">Workspace de análises</p><h2 className="text-3xl font-semibold tracking-[-0.04em]">Minhas análises</h2><p className="text-sm leading-6 text-muted-foreground">Continue uma conversa ou abra um novo fluxo de decisão com contexto preservado.</p></div>
      <div className="grid min-h-[650px] overflow-hidden rounded-xl border bg-card shadow-sm lg:grid-cols-[330px_1fr]">
        <aside className="flex min-h-0 flex-col border-b lg:border-b-0 lg:border-r"><div className="flex items-center justify-between p-4"><div><p className="text-sm font-semibold">Conversas</p><p className="text-xs text-muted-foreground">{chats.length} análises ativas</p></div><Button variant="ghost" size="icon" aria-label="Filtrar análises"><Filter /></Button></div><div className="px-4 pb-3"><div className="relative"><Search className="absolute left-3 top-2.5 size-4 text-muted-foreground" /><Input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Buscar análise" className="pl-9" /></div></div><ScrollArea className="flex-1"><div className="flex flex-col gap-1 p-2">{filtered.map((chat) => <button key={chat.id} type="button" onClick={() => setSelectedId(chat.id)} className={`flex flex-col gap-2 rounded-lg p-3 text-left transition-colors ${chat.id === selectedId ? "bg-primary/10" : "hover:bg-muted/50"}`}><div className="flex items-start gap-3"><div className="mt-0.5 flex size-8 shrink-0 items-center justify-center rounded-md bg-muted text-primary"><FileText className="size-4" /></div><div className="min-w-0 flex-1"><div className="flex items-center justify-between gap-2"><span className="truncate text-sm font-medium">{chat.title}</span>{chat.unread ? <Badge className="size-5 justify-center rounded-full p-0 text-[10px]">{chat.unread}</Badge> : null}</div><p className="mt-1 truncate text-xs text-muted-foreground">{chat.lastMessage}</p></div></div><div className="flex items-center justify-between pl-11 text-[10px] text-muted-foreground"><span>{chat.id}</span><span>{chat.updatedAt ?? "há 12 min"}</span></div></button>)}</div></ScrollArea></aside>
        <section className="flex min-h-0 flex-col"><header className="flex flex-col gap-3 border-b p-5 md:flex-row md:items-center md:justify-between"><div className="flex min-w-0 items-center gap-3"><div className="flex size-10 shrink-0 items-center justify-center rounded-lg bg-primary/10 text-primary"><MessageSquare className="size-5" /></div><div className="min-w-0"><div className="flex items-center gap-2"><h3 className="truncate text-base font-semibold">{selected?.title}</h3><Badge variant="secondary" className="shrink-0 bg-warning-soft text-warning-foreground">{selected?.status}</Badge></div><p className="mt-1 truncate text-xs text-muted-foreground">{selected?.orgao} · {selected?.id}</p></div></div><div className="flex items-center gap-2"><Button variant="outline" size="sm" render={<Link href={`/analyses/${selected?.id}`} />}><ArrowUpRight data-icon="inline-start" /> Abrir console</Button><Button variant="ghost" size="icon" aria-label="Mais opções"><MoreHorizontal /></Button></div></header><ScrollArea className="flex-1"><div className="mx-auto flex max-w-3xl flex-col gap-5 p-5 md:p-8"><div className="flex justify-center"><Badge variant="outline" className="gap-1.5"><Sparkles data-icon="inline-start" /> Contexto da análise preservado</Badge></div><div className="flex items-start gap-3"><div className="flex size-8 shrink-0 items-center justify-center rounded-full bg-primary text-primary-foreground"><Sparkles className="size-4" /></div><div className="flex max-w-2xl flex-col gap-3 rounded-xl rounded-tl-sm bg-muted/60 p-4"><p className="text-sm leading-6">Analisei os documentos vinculados a esta oportunidade. O risco geral está em <strong>{selected?.risk}/100</strong> e encontrei evidências que precisam de revisão antes da decisão.</p><div className="grid gap-2 sm:grid-cols-3"><div className="rounded-lg border bg-card p-3"><p className="text-[10px] uppercase tracking-wide text-muted-foreground">Risco</p><p className="mt-1 font-mono text-lg font-semibold">{selected?.risk}/100</p></div><div className="rounded-lg border bg-card p-3"><p className="text-[10px] uppercase tracking-wide text-muted-foreground">Evidências</p><p className="mt-1 font-mono text-lg font-semibold">12</p></div><div className="rounded-lg border bg-card p-3"><p className="text-[10px] uppercase tracking-wide text-muted-foreground">Política</p><p className="mt-1 truncate text-sm font-semibold">PR-04 v3.2</p></div></div></div></div><div className="flex items-start justify-end gap-3"><div className="max-w-xl rounded-xl rounded-tr-sm bg-primary p-4 text-primary-foreground"><p className="text-sm leading-6">Quais pontos devo validar primeiro?</p></div><div className="flex size-8 shrink-0 items-center justify-center rounded-full bg-muted text-xs font-semibold">MC</div></div><div className="flex items-start gap-3"><div className="flex size-8 shrink-0 items-center justify-center rounded-full bg-primary text-primary-foreground"><ShieldAlert className="size-4" /></div><div className="max-w-2xl rounded-xl rounded-tl-sm border p-4"><p className="text-sm leading-6">Comece pela garantia contratual na página 37 e pelo prazo de habilitação na página 11. Ambos têm impacto direto na recomendação.</p><div className="mt-3 flex flex-wrap gap-2"><Button variant="outline" size="sm"><ShieldAlert data-icon="inline-start" /> Ver achados críticos</Button><Button variant="outline" size="sm"><FileText data-icon="inline-start" /> Abrir página 37</Button></div></div></div><div className="flex items-center gap-3 text-xs text-muted-foreground"><Separator className="flex-1" /><span>hoje, 09:42</span><Separator className="flex-1" /></div><div className="flex flex-wrap gap-2"><Badge variant="outline" className="gap-1.5"><CheckCircle2 className="size-3 text-success" /> 42 páginas processadas</Badge><Badge variant="outline" className="gap-1.5"><Clock3 className="size-3 text-primary" /> Atualizado há 12 min</Badge></div></div></ScrollArea><footer className="border-t p-4"><div className="mx-auto flex max-w-3xl items-end gap-2 rounded-xl border bg-background p-2 shadow-sm"><Button variant="ghost" size="icon" aria-label="Anexar documento"><Paperclip /></Button><textarea aria-label="Mensagem para a análise" placeholder="Pergunte sobre evidências, risco ou política..." className="min-h-10 flex-1 resize-none bg-transparent px-2 py-2 text-sm outline-none placeholder:text-muted-foreground" /><Button size="icon" aria-label="Enviar mensagem"><Send /></Button></div><p className="mt-2 text-center text-[10px] text-muted-foreground">As respostas são baseadas nas evidências vinculadas à análise.</p></footer></section>
      </div>
    </div>
  </AppShell>
}

export const dynamic = "force-dynamic"

