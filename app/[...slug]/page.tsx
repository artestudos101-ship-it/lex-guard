"use client"

import Link from "next/link"
import { useState } from "react"
import { ArrowLeft, ArrowRight, CheckCircle2, Download, FileText, GitCompareArrows, LoaderCircle, Plus, Search, ShieldAlert, SlidersHorizontal } from "lucide-react"
import { AppShell } from "@/components/shell/app-shell"
import { Alert, AlertDescription, AlertTitle } from "@/components/ui/alert"
import { Badge } from "@/components/ui/badge"
import { Button } from "@/components/ui/button"
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"
import { Input } from "@/components/ui/input"
import { Progress } from "@/components/ui/progress"
import { Tabs, TabsContent, TabsList, TabsTrigger } from "@/components/ui/tabs"

const findings = [
  { title: "Exigência de atestado sem justificativa técnica", level: "Crítico", color: "bg-critical-soft text-critical-foreground", page: "p. 18", desc: "A cláusula exige experiência específica desproporcional ao objeto, reduzindo a competitividade do certame." },
  { title: "Prazo de vigência incompatível com política interna", level: "Atenção", color: "bg-warning-soft text-warning-foreground", page: "p. 27", desc: "O prazo de 48 meses excede o limite padrão de 36 meses definido na política PR-04." },
  { title: "Critério de reajuste identificado", level: "Informativo", color: "bg-info-soft text-info-foreground", page: "p. 31", desc: "Índice IPCA e data-base estão definidos de forma objetiva no instrumento convocatório." },
]

function NewAnalysis() {
  const [started, setStarted] = useState(false)
  return <AppShell title="Nova análise" description="Configure os documentos e parâmetros do processamento"><div className="mx-auto flex max-w-4xl flex-col gap-6"><div><Badge variant="outline" className="font-mono text-[10px] uppercase tracking-[0.16em]">Fluxo de ingestão</Badge><h2 className="mt-3 text-3xl font-semibold tracking-tight">Transforme um edital em uma decisão rastreável.</h2><p className="mt-2 text-sm leading-6 text-muted-foreground">O processamento é demonstrativo nesta versão. Os contratos estão preparados para conectar o pipeline de OCR e SSE.</p></div><Card><CardHeader><CardTitle className="text-base">1. Documentos fonte</CardTitle></CardHeader><CardContent className="flex flex-col gap-4"><div className="flex min-h-48 flex-col items-center justify-center gap-3 rounded-lg border border-dashed bg-muted/30 p-6 text-center"><FileText className="size-8 text-primary" /><span className="text-sm font-medium">Solte até 3 PDFs nesta área</span><span className="text-xs text-muted-foreground">Edital principal, anexos e termo de referência</span><Button variant="outline">Selecionar arquivos</Button></div><div className="flex items-center justify-between rounded-md border bg-card p-3"><div className="flex items-center gap-3"><div className="flex size-9 items-center justify-center rounded bg-critical-soft text-critical"><FileText className="size-4" /></div><div><p className="text-sm font-medium">edital_concorrencia_014.pdf</p><p className="text-xs text-muted-foreground">2,4 MB · 42 páginas</p></div></div><Badge variant="secondary">Pronto</Badge></div></CardContent></Card><Card><CardHeader><CardTitle className="text-base">2. Política de risco</CardTitle></CardHeader><CardContent className="grid gap-3 md:grid-cols-2"><button type="button" className="rounded-lg border-2 border-primary bg-primary/5 p-4 text-left"><span className="text-sm font-semibold">PR-04 · Contratações de tecnologia</span><span className="mt-1 block text-xs leading-5 text-muted-foreground">12 regras ativas · versão publicada em 12/06/2026</span></button><button type="button" className="rounded-lg border p-4 text-left transition-colors hover:border-primary"><span className="text-sm font-semibold">Política geral de compras</span><span className="mt-1 block text-xs leading-5 text-muted-foreground">8 regras ativas · versão publicada em 08/01/2026</span></button></CardContent></Card><div className="flex justify-end"><Button size="lg" onClick={() => setStarted(true)} disabled={started}>{started ? <><LoaderCircle data-icon="inline-start" className="animate-spin" /> Processando documentos</> : <>Iniciar análise <ArrowRight data-icon="inline-end" /></>}</Button></div>{started && <Alert><LoaderCircle className="animate-spin" /><AlertTitle>Processamento em andamento</AlertTitle><AlertDescription><div className="mt-3 flex flex-col gap-2"><div className="flex justify-between text-xs"><span>Extraindo cláusulas e evidências</span><span>64%</span></div><Progress value={64} /></div></AlertDescription></Alert>}</div></AppShell>
}

function DecisionConsole() { return <AppShell title="Console de decisão" description="AN-2026-014 · Concorrência 014/2026"><div className="mx-auto flex max-w-[1500px] flex-col gap-6"><div className="flex flex-col justify-between gap-4 md:flex-row md:items-end"><div><div className="flex items-center gap-2"><Badge className="bg-warning-soft text-warning-foreground" variant="secondary">Requer revisão</Badge><span className="font-mono text-xs text-muted-foreground">Atualizado agora</span></div><h2 className="mt-3 text-3xl font-semibold tracking-tight">Concorrência 014/2026 — Serviços de TI</h2><p className="mt-2 text-sm text-muted-foreground">Secretaria de Administração · Distrito Federal</p></div><div className="flex gap-2"><Button variant="outline" render={<Link href="/evidence/AN-2026-014" />}><FileText data-icon="inline-start" /> Ver evidências</Button><Button><Download data-icon="inline-start" /> Gerar pacote</Button></div></div><div className="grid gap-4 md:grid-cols-3"><Card><CardContent className="flex items-center gap-4 p-5"><div className="relative flex size-20 items-center justify-center rounded-full border-8 border-warning text-xl font-semibold">68</div><div><p className="text-sm font-medium">Risco geral</p><p className="mt-1 text-xs leading-5 text-muted-foreground">Acima do limiar recomendado de 50.</p></div></CardContent></Card><Card><CardContent className="flex items-center gap-4 p-5"><div className="flex size-12 items-center justify-center rounded-full bg-critical-soft text-critical"><ShieldAlert className="size-5" /></div><div><p className="text-2xl font-semibold">02</p><p className="text-xs text-muted-foreground">achados críticos</p></div></CardContent></Card><Card><CardContent className="flex items-center gap-4 p-5"><div className="flex size-12 items-center justify-center rounded-full bg-success-soft text-success"><CheckCircle2 className="size-5" /></div><div><p className="text-2xl font-semibold">86%</p><p className="text-xs text-muted-foreground">confiança das evidências</p></div></CardContent></Card></div><Tabs defaultValue="findings"><TabsList><TabsTrigger value="findings">Achados (8)</TabsTrigger><TabsTrigger value="comparison">Comparar política</TabsTrigger><TabsTrigger value="summary">Resumo executivo</TabsTrigger></TabsList><TabsContent value="findings" className="mt-5 flex flex-col gap-3"><div className="flex flex-col gap-3 rounded-lg border bg-card p-3 md:flex-row"><div className="relative flex-1"><Search className="absolute left-3 top-2.5 size-4 text-muted-foreground" /><Input className="pl-9" placeholder="Buscar em achados e evidências" /></div><Button variant="outline"><SlidersHorizontal data-icon="inline-start" /> Filtrar por severidade</Button></div>{findings.map((finding) => <Card key={finding.title}><CardContent className="flex flex-col gap-3 p-5 md:flex-row md:items-start md:justify-between"><div className="flex gap-3"><div className="mt-1 flex size-8 shrink-0 items-center justify-center rounded bg-critical-soft text-critical"><ShieldAlert className="size-4" /></div><div><div className="flex flex-wrap items-center gap-2"><h3 className="text-sm font-semibold">{finding.title}</h3><Badge variant="secondary" className={finding.color}>{finding.level}</Badge></div><p className="mt-2 max-w-3xl text-sm leading-6 text-muted-foreground">{finding.desc}</p><p className="mt-3 font-mono text-[10px] uppercase tracking-wider text-muted-foreground">Edital · {finding.page} · cláusula 7.2</p></div></div><Button variant="ghost" size="sm" render={<Link href="/evidence/AN-2026-014" />}>Abrir evidência <ArrowRight data-icon="inline-end" /></Button></CardContent></Card>)}</TabsContent><TabsContent value="comparison" className="mt-5"><Card><CardContent className="flex min-h-56 flex-col items-center justify-center gap-3 p-8 text-center"><GitCompareArrows className="size-8 text-primary" /><h3 className="font-semibold">Comparação política vs. edital</h3><p className="max-w-md text-sm leading-6 text-muted-foreground">3 conflitos encontrados entre as regras PR-04 e as cláusulas analisadas.</p><Button>Ver conflitos</Button></CardContent></Card></TabsContent><TabsContent value="summary" className="mt-5"><Card><CardContent className="p-6 text-sm leading-7 text-muted-foreground">A análise identificou risco moderado-alto, concentrado em requisitos de habilitação e prazo contratual. Recomenda-se revisão jurídica antes da publicação.</CardContent></Card></TabsContent></Tabs></div></AppShell> }

function GenericPage({ name }: { name: string }) { const title = name === "analyses" ? "Análises" : name === "policies" ? "Políticas de risco" : name === "reports" ? "Pacotes de decisão" : "Avaliação"; return <AppShell title={title} description="Dados simulados para demonstração"><div className="mx-auto flex max-w-6xl flex-col gap-6"><div className="flex items-end justify-between"><div><h2 className="text-3xl font-semibold tracking-tight">{title}</h2><p className="mt-2 text-sm text-muted-foreground">Organize o contexto e acompanhe as decisões do seu time.</p></div><Button render={<Link href="/analyses/new" />}><Plus data-icon="inline-start" /> Nova análise</Button></div><Card><CardContent className="flex min-h-72 flex-col items-center justify-center gap-3 p-8 text-center"><CheckCircle2 className="size-8 text-success" /><h3 className="font-semibold">Área pronta para conexão</h3><p className="max-w-md text-sm leading-6 text-muted-foreground">Este módulo usa os contratos do domínio e pode receber dados reais da API sem mudar a interface.</p><Button variant="outline" render={<Link href="/" />}>Voltar ao painel</Button></CardContent></Card></div></AppShell> }

export default function SlugPage({ params }: { params: Promise<{ slug?: string[] }> }) { const [slug, setSlug] = useState<string[] | null>(null); if (!slug) { params.then((value) => setSlug(value.slug ?? [])); return null } const key = slug[0] ?? ""; if (key === "analyses" && slug[1] === "new") return <NewAnalysis />; if (key === "analyses" && slug[1]) return <DecisionConsole />; if (key === "evidence") return <DecisionConsole />; return <GenericPage name={key} /> }
