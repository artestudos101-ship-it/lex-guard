"use client"
import Link from "next/link"
import { use, useState } from "react"
import { Activity, ArrowRight, CheckCircle2, Clock3, Download, FileSearch, FileText, Filter, MessageSquare, Plus, Search, ShieldAlert, Users, XCircle } from "lucide-react"
import { AppShell } from "@/components/shell/app-shell"
import { Badge } from "@/components/ui/badge"
import { Button } from "@/components/ui/button"
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"
import { Input } from "@/components/ui/input"
import { Progress } from "@/components/ui/progress"
import { Separator } from "@/components/ui/separator"
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from "@/components/ui/table"
import { DEMO_ACTIVITY, DEMO_PROCESSING, DEMO_TEAMS, DEMO_USERS, ROLE_LABELS, STATUS_LABELS } from "@/types/organization"
import { MOCK_ANALYSES } from "@/mock/analyses"
import { OperationalModule } from "@/components/modules/operational-module"
import { NewAnalysis } from "@/components/analyses/new-analysis"
import { SearchPage } from "@/components/modules/search-page"
import { SettingsPage } from "@/components/modules/settings-page"
import { AnalysisWorkspace } from "@/features/analyses/components/analysis-workspace"

const tone = { warning: "bg-warning-soft text-warning-foreground", success: "bg-success-soft text-success-foreground", info: "bg-info-soft text-info-foreground", critical: "bg-critical-soft text-critical-foreground" }
function Header({ title, description, cta = "Nova análise" }: { title: string; description: string; cta?: string }) { return <div className="flex flex-col justify-between gap-4 md:flex-row md:items-end"><div><p className="font-mono text-[10px] uppercase tracking-[0.18em] text-primary">LexGuard · 26 ago 2026</p><h2 className="mt-2 text-3xl font-semibold tracking-[-0.04em] text-balance">{title}</h2><p className="mt-2 text-sm leading-6 text-muted-foreground">{description}</p></div>{cta ? <Button render={<Link href="/analyses/new" />}><Plus data-icon="inline-start" />{cta}</Button> : null}</div> }
<<<<<<< HEAD
function Dashboard() { return <AppShell title="Painel de decisão" description="Visão geral do portfólio de análises"><div className="mx-auto flex max-w-[1500px] flex-col gap-6"><Header title="Decida com evidências, não com suposições." description="Acompanhe risco, capacidade do time e próximos pontos de decisão em um só lugar." /><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-4">{([{label:"Análises em andamento",value:"04",detail:"+2 nesta semana",Icon:Activity},{label:"Risco médio do portfólio",value:"42/100",detail:"↓ 8 pontos vs. mês anterior",Icon:ShieldAlert},{label:"Evidências processadas",value:"1.284",detail:"98,4% com confiança alta",Icon:FileText},{label:"Tempo médio de decisão",value:"18 min",detail:"↓ 32% vs. fluxo manual",Icon:Clock3}]).map(({label,value,detail,Icon}) => <Card key={String(label)}><CardContent className="flex items-start justify-between p-5"><div className="flex flex-col gap-2"><span className="text-xs text-muted-foreground">{label}</span><strong className="text-2xl tracking-tight">{value}</strong><span className="text-xs text-success">{detail}</span></div><Icon className="size-5 text-primary" /></CardContent></Card>)}</div><div className="grid gap-6 xl:grid-cols-[1.4fr_.6fr]"><Card><CardHeader className="border-b"><CardTitle className="text-base">Prioridades do time</CardTitle></CardHeader><CardContent className="flex flex-col gap-1 p-0">{MOCK_ANALYSES.map((a, i) => <Link key={a.id} href={`/analyses/${a.id}`} className="flex items-center gap-4 border-b p-5 transition-colors last:border-0 hover:bg-muted/40"><div className="flex size-9 shrink-0 items-center justify-center rounded-md bg-primary/10 text-primary"><FileSearch className="size-4" /></div><div className="min-w-0 flex-1"><p className="truncate text-sm font-medium">{a.title}</p><p className="mt-1 text-xs text-muted-foreground">{a.orgao} · atualizado {i ? "ontem" : "há 12 min"}</p></div><Badge variant="secondary" className={tone[i === 0 ? "warning" : i === 1 ? "success" : "critical"]}>{i === 0 ? "Requer revisão" : i === 1 ? "Concluída" : "Alta prioridade"}</Badge><ArrowRight className="size-4 text-muted-foreground" /></Link>)}</CardContent></Card><Card><CardHeader><CardTitle className="text-base">Atividade recente</CardTitle></CardHeader><CardContent className="flex flex-col gap-5">{DEMO_ACTIVITY.map((event) => <div className="flex gap-3" key={event.id}><div className={`mt-1 size-2 rounded-full ${event.tone === "warning" ? "bg-warning" : event.tone === "success" ? "bg-success" : "bg-info"}`} /><div><p className="text-sm"><strong>{event.actor}</strong> {event.action}</p><p className="mt-1 text-xs text-muted-foreground">{event.target} · {event.time}</p></div></div>)}</CardContent></Card></div></div></AppShell> }
function Analyses() { return <AppShell title="Análises" description="Fila de decisões do seu tenant"><div className="mx-auto flex max-w-6xl flex-col gap-6"><Header title="Todas as análises" description="Filtre, atribua e acompanhe o ciclo completo de cada edital." /><Card><CardContent className="flex flex-col gap-4 p-4"><div className="flex flex-col gap-3 md:flex-row"><div className="relative flex-1"><Search className="absolute left-3 top-2.5 size-4 text-muted-foreground" /><Input className="pl-9" placeholder="Buscar por título, órgão ou código" /></div><Button variant="outline"><Filter data-icon="inline-start" /> Filtros</Button></div><Table><TableHeader><TableRow><TableHead>Análise</TableHead><TableHead>Status</TableHead><TableHead>Risco</TableHead><TableHead>Atualização</TableHead><TableHead /></TableRow></TableHeader><TableBody>{MOCK_ANALYSES.map((a, i) => <TableRow key={a.id}><TableCell><Link className="font-medium hover:underline" href={`/analyses/${a.id}`}>{a.title}</Link><p className="text-xs text-muted-foreground">{a.id} · {a.orgao}</p></TableCell><TableCell><Badge variant="secondary" className={tone[i === 0 ? "warning" : "success"]}>{i === 0 ? "Em revisão" : "Concluída"}</Badge></TableCell><TableCell><span className="font-mono">{[68,24,81][i]}/100</span></TableCell><TableCell className="text-muted-foreground">{i === 0 ? "há 12 min" : "ontem"}</TableCell><TableCell><Button variant="ghost" size="sm" render={<Link href={`/analyses/${a.id}`} />}>Abrir <ArrowRight data-icon="inline-end" /></Button></TableCell></TableRow>)}</TableBody></Table></CardContent></Card></div></AppShell> }
=======
function Dashboard() { return <AppShell title="Painel de decisão" description="Visão geral do portfólio de análises"><div className="mx-auto flex max-w-[1500px] flex-col gap-6"><Header title="Decida com evidências, não com suposições." description="Acompanhe risco, capacidade do time e próximos pontos de decisão em um só lugar." /><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-4">{([{ label: "Análises em andamento", value: "04", detail: "+2 nesta semana", Icon: Activity }, { label: "Risco médio do portfólio", value: "42/100", detail: "↓ 8 pontos vs. mês anterior", Icon: ShieldAlert }, { label: "Evidências processadas", value: "1.284", detail: "98,4% com confiança alta", Icon: FileText }, { label: "Tempo médio de decisão", value: "18 min", detail: "↓ 32% vs. fluxo manual", Icon: Clock3 }]).map(({ label, value, detail, Icon }) => <Card key={String(label)}><CardContent className="flex items-start justify-between p-5"><div className="flex flex-col gap-2"><span className="text-xs text-muted-foreground">{label}</span><strong className="text-2xl tracking-tight">{value}</strong><span className="text-xs text-success">{detail}</span></div><Icon className="size-5 text-primary" /></CardContent></Card>)}</div><div className="grid gap-6 xl:grid-cols-[1.4fr_.6fr]"><Card><CardHeader className="border-b"><CardTitle className="text-base">Prioridades do time</CardTitle></CardHeader><CardContent className="flex flex-col gap-1 p-0">{MOCK_ANALYSES.map((a, i) => <Link key={a.id} href={`/analyses/${a.id}`} className="flex items-center gap-4 border-b p-5 transition-colors last:border-0 hover:bg-muted/40"><div className="flex size-9 shrink-0 items-center justify-center rounded-md bg-primary/10 text-primary"><FileSearch className="size-4" /></div><div className="min-w-0 flex-1"><p className="truncate text-sm font-medium">{a.title}</p><p className="mt-1 text-xs text-muted-foreground">{a.orgao} · atualizado {i ? "ontem" : "há 12 min"}</p></div><Badge variant="secondary" className={tone[i === 0 ? "warning" : i === 1 ? "success" : "critical"]}>{i === 0 ? "Requer revisão" : i === 1 ? "Concluída" : "Alta prioridade"}</Badge><ArrowRight className="size-4 text-muted-foreground" /></Link>)}</CardContent></Card><Card><CardHeader><CardTitle className="text-base">Atividade recente</CardTitle></CardHeader><CardContent className="flex flex-col gap-5">{DEMO_ACTIVITY.map((event) => <div className="flex gap-3" key={event.id}><div className={`mt-1 size-2 rounded-full ${event.tone === "warning" ? "bg-warning" : event.tone === "success" ? "bg-success" : "bg-info"}`} /><div><p className="text-sm"><strong>{event.actor}</strong> {event.action}</p><p className="mt-1 text-xs text-muted-foreground">{event.target} · {event.time}</p></div></div>)}</CardContent></Card></div></div></AppShell> }
function Analyses() { return <AppShell title="Análises" description="Fila de decisões do seu tenant"><div className="mx-auto flex max-w-6xl flex-col gap-6"><Header title="Todas as análises" description="Filtre, atribua e acompanhe o ciclo completo de cada edital." /><Card><CardContent className="flex flex-col gap-4 p-4"><div className="flex flex-col gap-3 md:flex-row"><div className="relative flex-1"><Search className="absolute left-3 top-2.5 size-4 text-muted-foreground" /><Input className="pl-9" placeholder="Buscar por título, órgão ou código" /></div><Button variant="outline"><Filter data-icon="inline-start" /> Filtros</Button></div><Table><TableHeader><TableRow><TableHead>Análise</TableHead><TableHead>Status</TableHead><TableHead>Risco</TableHead><TableHead>Atualização</TableHead><TableHead /></TableRow></TableHeader><TableBody>{MOCK_ANALYSES.map((a, i) => <TableRow key={a.id}><TableCell><Link className="font-medium hover:underline" href={`/analyses/${a.id}`}>{a.title}</Link><p className="text-xs text-muted-foreground">{a.id} · {a.orgao}</p></TableCell><TableCell><Badge variant="secondary" className={tone[i === 0 ? "warning" : "success"]}>{i === 0 ? "Em revisão" : "Concluída"}</Badge></TableCell><TableCell><span className="font-mono">{[68, 24, 81][i]}/100</span></TableCell><TableCell className="text-muted-foreground">{i === 0 ? "há 12 min" : "ontem"}</TableCell><TableCell><Button variant="ghost" size="sm" render={<Link href={`/analyses/${a.id}`} />}>Abrir <ArrowRight data-icon="inline-end" /></Button></TableCell></TableRow>)}</TableBody></Table></CardContent></Card></div></AppShell> }
>>>>>>> c2b5b919d787ff80bc8acf75c985ad5beb80db5c
function Teams() { return <AppShell title="Times" description="Estrutura de colaboração do tenant"><div className="mx-auto flex max-w-6xl flex-col gap-6"><Header title="Times e responsabilidades" description="Organize quem revisa, decide e acompanha os editais." cta="Convidar membro" /><div className="grid gap-4 md:grid-cols-3">{DEMO_TEAMS.map((team) => <Card key={team.id}><CardHeader><div className="flex items-center justify-between"><div className={`size-3 rounded-full ${team.color}`} /><Badge variant="outline">{team.memberCount} membros</Badge></div><CardTitle className="text-base">{team.name}</CardTitle></CardHeader><CardContent><p className="text-sm leading-6 text-muted-foreground">{team.description}</p><Button variant="link" className="mt-4 h-auto p-0" render={<Link href={`/teams/${team.id}`} />}>Ver time <ArrowRight data-icon="inline-end" /></Button></CardContent></Card>)}</div></div></AppShell> }
function UsersPage() { return <AppShell title="Usuários" description="Acessos e papéis do tenant"><div className="mx-auto flex max-w-6xl flex-col gap-6"><Header title="Membros do tenant" description="Controle papéis, status e última atividade." cta="Convidar usuário" /><Card><CardContent className="p-0"><Table><TableHeader><TableRow><TableHead>Pessoa</TableHead><TableHead>Time</TableHead><TableHead>Papel</TableHead><TableHead>Status</TableHead><TableHead>Atividade</TableHead></TableRow></TableHeader><TableBody>{DEMO_USERS.map((user) => <TableRow key={user.id}><TableCell><div className="flex items-center gap-3"><div className="flex size-8 items-center justify-center rounded-full bg-primary/10 text-xs font-medium text-primary">{user.initials}</div><div><p className="font-medium">{user.name}</p><p className="text-xs text-muted-foreground">{user.email}</p></div></div></TableCell><TableCell>{user.team}</TableCell><TableCell><Badge variant="outline">{ROLE_LABELS[user.role]}</Badge></TableCell><TableCell><Badge variant="secondary" className={user.status === "active" ? tone.success : tone.info}>{user.status === "active" ? "Ativo" : "Convite enviado"}</Badge></TableCell><TableCell className="text-muted-foreground">{user.lastActive}</TableCell></TableRow>)}</TableBody></Table></CardContent></Card></div></AppShell> }
function ActivityPage() { return <AppShell title="Atividade" description="Trilha auditável de ações do tenant" cta=""><div className="mx-auto flex max-w-4xl flex-col gap-6"><Header title="Trilha de atividade" description="Histórico de decisões, documentos e colaboração." cta="" /><Card><CardContent className="flex flex-col gap-6 p-6">{[...DEMO_ACTIVITY, ...DEMO_ACTIVITY].map((event, i) => <div key={`${event.id}-${i}`} className="flex gap-4"><div className="flex flex-col items-center"><div className={`mt-1 size-3 rounded-full ${event.tone === "warning" ? "bg-warning" : event.tone === "success" ? "bg-success" : "bg-info"}`} />{i < 5 ? <div className="mt-2 h-10 w-px bg-border" /> : null}</div><div><p className="text-sm"><strong>{event.actor}</strong> {event.action}</p><p className="mt-1 text-xs text-muted-foreground">{event.target} · {event.time}</p></div></div>)}</CardContent></Card></div></AppShell> }
function Processing() { const [progress, setProgress] = useState(72); return <AppShell title="Processamento" description="AN-2026-014 · eventos observáveis" cta=""><div className="mx-auto flex max-w-6xl flex-col gap-6"><Header title="Análise em processamento" description="O LexGuard está transformando documentos em evidências rastreáveis." cta="" /><Card><CardContent className="flex flex-col gap-6 p-6"><div className="flex items-center justify-between"><div><p className="text-sm font-medium">Progresso geral</p><p className="mt-1 text-xs text-muted-foreground">Classificação de cláusulas · 3 de 5 etapas</p></div><span className="font-mono text-sm">{progress}%</span></div><Progress value={progress} /><div className="flex flex-col gap-1">{DEMO_PROCESSING.map((event) => <div key={event.id} className="flex items-center gap-4 border-b py-4 last:border-0"><div className={`flex size-8 items-center justify-center rounded-full ${event.status === "done" ? tone.success : event.status === "active" ? tone.info : event.status === "error" ? tone.critical : "bg-muted text-muted-foreground"}`}>{event.status === "done" ? <CheckCircle2 className="size-4" /> : event.status === "error" ? <XCircle className="size-4" /> : <Activity className="size-4" />}</div><div className="flex-1"><p className="text-sm font-medium">{event.label}</p><p className="text-xs text-muted-foreground">{event.detail}</p></div><Badge variant="outline">{STATUS_LABELS[event.status]}</Badge></div>)}</div><Button onClick={() => setProgress(100)} className="self-end">Simular conclusão <ArrowRight data-icon="inline-end" /></Button></CardContent></Card></div></AppShell> }
<<<<<<< HEAD
function Detail() { return <AppShell title="Console de decisão" description="AN-2026-014 · Concorrência 014/2026"><div className="mx-auto flex max-w-[1500px] flex-col gap-6"><Header title="Concorrência 014/2026 — Serviços de TI" description="Secretaria de Administração · Distrito Federal" cta="Gerar pacote" /><div className="grid gap-4 md:grid-cols-3"><Card><CardContent className="flex items-center gap-4 p-5"><div className="flex size-20 items-center justify-center rounded-full border-8 border-warning text-xl font-semibold">68</div><div><p className="text-sm font-medium">Risco geral</p><p className="mt-1 text-xs text-muted-foreground">Acima do limiar recomendado de 50.</p></div></CardContent></Card><Card><CardContent className="flex items-center gap-4 p-5"><div className="flex size-12 items-center justify-center rounded-full bg-critical-soft text-critical"><ShieldAlert className="size-5" /></div><div><p className="text-2xl font-semibold">02</p><p className="text-xs text-muted-foreground">achados críticos</p></div></CardContent></Card><Card><CardContent className="flex items-center gap-4 p-5"><div className="flex size-12 items-center justify-center rounded-full bg-success-soft text-success"><CheckCircle2 className="size-5" /></div><div><p className="text-2xl font-semibold">86%</p><p className="text-xs text-muted-foreground">confiança média</p></div></CardContent></Card></div><div className="grid gap-6 xl:grid-cols-[1fr_.38fr]"><Card><CardHeader className="flex flex-row items-center justify-between border-b"><div><CardTitle className="text-base">Achados priorizados</CardTitle><p className="mt-1 text-sm text-muted-foreground">Evidências Alpha, Beta e Gamma vinculadas ao documento.</p></div><Badge className={tone.warning} variant="secondary">REVISAR</Badge></CardHeader><CardContent className="flex flex-col gap-4 p-5">{[{title:"Garantia contratual elevada",level:"Crítico",page:"p. 37",desc:"O edital exige garantia de execução de 5% do valor total, acima do limite de 3% da política PR-04."},{title:"Prazo de proposta reduzido",level:"Atenção",page:"p. 11",desc:"Apenas 8 dias úteis para apresentação, elevando risco de erro na composição de custos."},{title:"Multa moratória compatível",level:"Informativo",page:"p. 38",desc:"Percentual de 8% dentro do teto de 10% da política vigente."}].map((f) => <div key={f.title} className="rounded-lg border p-4"><div className="flex items-start justify-between gap-3"><div><div className="flex items-center gap-2"><Badge variant="secondary" className={f.level === "Crítico" ? tone.critical : f.level === "Atenção" ? tone.warning : tone.info}>{f.level}</Badge><span className="font-mono text-[10px] text-muted-foreground">{f.page}</span></div><h3 className="mt-3 text-sm font-semibold">{f.title}</h3></div><Button variant="ghost" size="icon" aria-label={`Comentar sobre ${f.title}`}><MessageSquare /></Button></div><p className="mt-2 text-sm leading-6 text-muted-foreground">{f.desc}</p><Separator className="my-4" /><div className="flex flex-wrap items-center gap-2 text-xs"><Badge variant="outline">E2 · Confiança média</Badge><Button variant="link" className="h-auto p-0">Ver evidência <ArrowRight data-icon="inline-end" /></Button></div></div>)}</CardContent></Card><Card><CardHeader><CardTitle className="text-base">Colaboração</CardTitle></CardHeader><CardContent className="flex flex-col gap-5"><div><p className="text-xs text-muted-foreground">Responsável</p><p className="mt-1 text-sm font-medium">Marina Costa</p></div><div><p className="text-xs text-muted-foreground">Status</p><Badge className={`mt-2 ${tone.warning}`} variant="secondary">Em revisão</Badge></div><div><p className="text-xs text-muted-foreground">Último comentário</p><p className="mt-2 text-sm leading-6">Sugiro confirmar a alternativa de seguro-garantia com o comercial.</p></div><Button variant="outline"><Users data-icon="inline-start" /> Atribuir revisão</Button><Button><Download data-icon="inline-start" /> Exportar pacote</Button></CardContent></Card></div></div></AppShell> }
function Generic({ name }: { name: string }) { const titles: Record<string, [string,string]> = { policies: ["Políticas de risco", "Regras que orientam cada decisão."], reports: ["Pacotes de decisão", "Relatórios rastreáveis prontos para compartilhar."], evaluation: ["Avaliação", "Meça precisão, cobertura e qualidade das evidências."], search: ["Busca global", "Encontre análises, políticas e pessoas dentro do tenant."], notifications: ["Notificações", "Acompanhe tarefas e eventos importantes."], settings: ["Configurações", "Preferências do tenant e controles de acesso."] }; const [title, desc] = titles[name] ?? ["Área do produto", "Módulo LexGuard preparado para conexão com a API."]; return <AppShell title={title} description={desc}><div className="mx-auto flex max-w-5xl flex-col gap-6"><Header title={title} description={desc} cta={name === "policies" ? "Nova política" : ""} /><Card><CardContent className="flex min-h-64 flex-col items-center justify-center gap-3 p-8 text-center"><CheckCircle2 className="size-8 text-success" /><h3 className="font-semibold">Módulo pronto para operação</h3><p className="max-w-md text-sm leading-6 text-muted-foreground">Dados do tenant, histórico e permissões aparecem aqui com o mesmo contrato que será usado pela API.</p><Button variant="outline" render={<Link href="/" />}>Voltar ao painel</Button></CardContent></Card></div></AppShell> }
=======
function Detail() { return <AppShell title="Console de decisão" description="AN-2026-014 · Concorrência 014/2026"><div className="mx-auto flex max-w-[1500px] flex-col gap-6"><Header title="Concorrência 014/2026 — Serviços de TI" description="Secretaria de Administração · Distrito Federal" cta="Gerar pacote" /><div className="grid gap-4 md:grid-cols-3"><Card><CardContent className="flex items-center gap-4 p-5"><div className="flex size-20 items-center justify-center rounded-full border-8 border-warning text-xl font-semibold">68</div><div><p className="text-sm font-medium">Risco geral</p><p className="mt-1 text-xs text-muted-foreground">Acima do limiar recomendado de 50.</p></div></CardContent></Card><Card><CardContent className="flex items-center gap-4 p-5"><div className="flex size-12 items-center justify-center rounded-full bg-critical-soft text-critical"><ShieldAlert className="size-5" /></div><div><p className="text-2xl font-semibold">02</p><p className="text-xs text-muted-foreground">achados críticos</p></div></CardContent></Card><Card><CardContent className="flex items-center gap-4 p-5"><div className="flex size-12 items-center justify-center rounded-full bg-success-soft text-success"><CheckCircle2 className="size-5" /></div><div><p className="text-2xl font-semibold">86%</p><p className="text-xs text-muted-foreground">confiança média</p></div></CardContent></Card></div><div className="grid gap-6 xl:grid-cols-[1fr_.38fr]"><Card><CardHeader className="flex flex-row items-center justify-between border-b"><div><CardTitle className="text-base">Achados priorizados</CardTitle><p className="mt-1 text-sm text-muted-foreground">Evidências Alpha, Beta e Gamma vinculadas ao documento.</p></div><Badge className={tone.warning} variant="secondary">REVISAR</Badge></CardHeader><CardContent className="flex flex-col gap-4 p-5">{[{ title: "Garantia contratual elevada", level: "Crítico", page: "p. 37", desc: "O edital exige garantia de execução de 5% do valor total, acima do limite de 3% da política PR-04." }, { title: "Prazo de proposta reduzido", level: "Atenção", page: "p. 11", desc: "Apenas 8 dias úteis para apresentação, elevando risco de erro na composição de custos." }, { title: "Multa moratória compatível", level: "Informativo", page: "p. 38", desc: "Percentual de 8% dentro do teto de 10% da política vigente." }].map((f) => <div key={f.title} className="rounded-lg border p-4"><div className="flex items-start justify-between gap-3"><div><div className="flex items-center gap-2"><Badge variant="secondary" className={f.level === "Crítico" ? tone.critical : f.level === "Atenção" ? tone.warning : tone.info}>{f.level}</Badge><span className="font-mono text-[10px] text-muted-foreground">{f.page}</span></div><h3 className="mt-3 text-sm font-semibold">{f.title}</h3></div><Button variant="ghost" size="icon" aria-label={`Comentar sobre ${f.title}`}><MessageSquare /></Button></div><p className="mt-2 text-sm leading-6 text-muted-foreground">{f.desc}</p><Separator className="my-4" /><div className="flex flex-wrap items-center gap-2 text-xs"><Badge variant="outline">E2 · Confiança média</Badge><Button variant="link" className="h-auto p-0">Ver evidência <ArrowRight data-icon="inline-end" /></Button></div></div>)}</CardContent></Card><Card><CardHeader><CardTitle className="text-base">Colaboração</CardTitle></CardHeader><CardContent className="flex flex-col gap-5"><div><p className="text-xs text-muted-foreground">Responsável</p><p className="mt-1 text-sm font-medium">Marina Costa</p></div><div><p className="text-xs text-muted-foreground">Status</p><Badge className={`mt-2 ${tone.warning}`} variant="secondary">Em revisão</Badge></div><div><p className="text-xs text-muted-foreground">Último comentário</p><p className="mt-2 text-sm leading-6">Sugiro confirmar a alternativa de seguro-garantia com o comercial.</p></div><Button variant="outline"><Users data-icon="inline-start" /> Atribuir revisão</Button><Button><Download data-icon="inline-start" /> Exportar pacote</Button></CardContent></Card></div></div></AppShell> }
function Generic({ name }: { name: string }) { const titles: Record<string, [string, string]> = { policies: ["Políticas de risco", "Regras que orientam cada decisão."], reports: ["Pacotes de decisão", "Relatórios rastreáveis prontos para compartilhar."], evaluation: ["Avaliação", "Meça precisão, cobertura e qualidade das evidências."], search: ["Busca global", "Encontre análises, políticas e pessoas dentro do tenant."], notifications: ["Notificações", "Acompanhe tarefas e eventos importantes."], settings: ["Configurações", "Preferências do tenant e controles de acesso."] }; const [title, desc] = titles[name] ?? ["Área do produto", "Módulo LexGuard preparado para conexão com a API."]; return <AppShell title={title} description={desc}><div className="mx-auto flex max-w-5xl flex-col gap-6"><Header title={title} description={desc} cta={name === "policies" ? "Nova política" : ""} /><Card><CardContent className="flex min-h-64 flex-col items-center justify-center gap-3 p-8 text-center"><CheckCircle2 className="size-8 text-success" /><h3 className="font-semibold">Módulo pronto para operação</h3><p className="max-w-md text-sm leading-6 text-muted-foreground">Dados do tenant, histórico e permissões aparecem aqui com o mesmo contrato que será usado pela API.</p><Button variant="outline" render={<Link href="/" />}>Voltar ao painel</Button></CardContent></Card></div></AppShell> }
>>>>>>> c2b5b919d787ff80bc8acf75c985ad5beb80db5c
export default function SlugPage({ params }: { params: Promise<{ slug?: string[] }> }) { const { slug = [] } = use(params); const key = slug[0] ?? ""; if (!key) return <Dashboard />; if (key === "analyses" && slug[1] === "new") return <NewAnalysis />; if (key === "analyses" && slug[1] === "an_demo" && slug[2] === "processing") return <Processing />; if (key === "analyses" && slug[1]) return <AnalysisWorkspace analysisId={slug[1]} />; if (key === "analyses") return <Analyses />; if (key === "teams" && slug[1]) return <Generic name="teams" />; if (key === "teams") return <Teams />; if (key === "users") return <UsersPage />; if (key === "activity") return <ActivityPage />; if (key === "search") return <SearchPage />; if (key === "settings") return <SettingsPage />; if (["policies", "reports", "evaluation", "notifications"].includes(key)) return <OperationalModule name={key} />; return <Generic name={key} /> }
